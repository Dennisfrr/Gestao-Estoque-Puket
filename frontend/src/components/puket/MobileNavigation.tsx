import { History, MessageCircleMore, Settings2, ShoppingBag, Sparkles, Waypoints } from "lucide-react";
import { Link, useRouterState } from "@tanstack/react-router";
const items = [{ to: "/", label: "Automatizar", icon: Sparkles }, { to: "/copiloto", label: "Copiloto", icon: MessageCircleMore }, { to: "/catalogo", label: "Catálogo", icon: ShoppingBag }, { to: "/historico", label: "Histórico", icon: History }, { to: "/status", label: "Status", icon: Waypoints }, { to: "/configuracoes", label: "Ajustes", icon: Settings2 }] as const;
export function MobileNavigation() { const pathname = useRouterState({ select: (state) => state.location.pathname }); return <nav className="fixed inset-x-0 bottom-0 z-40 grid grid-cols-6 border-t bg-card/95 px-1 pb-[max(.5rem,env(safe-area-inset-bottom))] pt-2 backdrop-blur lg:hidden" aria-label="Navegação principal">{items.map(({ to, label, icon: Icon }) => { const active = to === "/" ? pathname === "/" : pathname.startsWith(to); return <Link key={to} to={to} className={`flex min-h-14 flex-col items-center justify-center gap-1 rounded-xl px-0.5 text-[9px] font-bold ${active ? "bg-primary-soft text-primary" : "text-muted-foreground"}`}><Icon className="h-5 w-5" aria-hidden="true" />{label}</Link>; })}</nav>; }
